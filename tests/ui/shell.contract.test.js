const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const shell=fs.readFileSync(path.join(__dirname,'../../src/projectMaverick/ui/shell.js'),'utf8'),css=fs.readFileSync(path.join(__dirname,'../../src/projectMaverick/styles/projectMaverick.css'),'utf8'),components=fs.readFileSync(path.join(__dirname,'../../src/projectMaverick/ui/components.js'),'utf8');test('shell mounts one prefixed root and is idempotent',()=>{assert.match(shell,/getElementById\('pm-root'\)/);assert.match(shell,/root\.id='pm-root'/);});test('native text uses textContent',()=>{assert.match(components,/textContent/);assert.doesNotMatch(components,/\.innerHTML\s*=/);});test('stylesheet uses Project Maverick prefixes',()=>{for(const selector of css.matchAll(/([.#])([A-Za-z0-9_-]+)/g))assert.ok(selector[2].startsWith('pm-'),selector[2]);});test('shell supports keyboard close and focus return',()=>{assert.match(shell,/Escape/);assert.match(shell,/lastFocus/);});
