const test=require('node:test'),assert=require('node:assert/strict');const{scorePolitician,distance}=require('../../src/projectMaverick/ideology/scoring');const{DIMENSIONS}=require('../../src/projectMaverick/ideology/dimensions');test('progressive evidence',()=>{const r=scorePolitician({fiscalScore:-.7,socialScore:-.8,issues:{taxes:-.8,healthcare:-1,guns:-.7,immigration:-.8,environment:-.9,education:-.5}});assert.ok(r.dimensions.economics<0&&r.dimensions.social<0&&r.dimensions.healthcare<0&&r.confidence>.5);});test('libertarian evidence',()=>{const r=scorePolitician({fiscalScore:.8,socialScore:-.7,issues:{taxes:.9,guns:.9,immigration:-.6,healthcare:.7}});assert.ok(r.dimensions.economics>0&&r.dimensions.social<0&&r.dimensions.guns>0);});test('cross pressured preserved',()=>{const r=scorePolitician({fiscalScore:-.7,socialScore:.8,issues:{welfare:-.8,immigration:.8}});assert.ok(r.dimensions.economics<-.3&&r.dimensions.social>.3);});test('sparse low confidence',()=>{const r=scorePolitician({fiscalScore:.2});assert.deepEqual(Object.keys(r.dimensions).sort(),[...DIMENSIONS].sort());assert.ok(r.confidence<.4);});test('distance bounded symmetric',()=>{const a=Object.fromEntries(DIMENSIONS.map(k=>[k,-1])),b=Object.fromEntries(DIMENSIONS.map(k=>[k,1]));assert.equal(distance(a,b),2);assert.equal(distance(a,b),distance(b,a));});
