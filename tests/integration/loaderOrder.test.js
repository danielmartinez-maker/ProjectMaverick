const test=require('node:test'),assert=require('node:assert/strict'),fs=require('node:fs'),os=require('node:os'),path=require('node:path');const{build}=require('../../tools/build');test('loader dependency order is stable and duplicate guarded',()=>{const s=fs.readFileSync(path.join(__dirname,'../../src/projectMaverick/loader.js'),'utf8');assert.match(s,/__booted/);const tokens=['core/namespace','core/diagnostics','core/compatibility','core/storage','core/events','core/nativeAdapter','ideology/scoring','ideology/classifyPolitician','archetypes/classifyPoliticalStyle','voters/cohortModel','qol/search','ui/shell'];let last=-1;for(const token of tokens){const i=s.indexOf(token);assert.ok(i>last,`${token} should appear after previous module`);last=i;}});test('build emits source-only allowlisted payload',()=>{const out=fs.mkdtempSync(path.join(os.tmpdir(),'pm-build-')),r=build(out);assert.equal(r.prohibited.length,0);assert.ok(r.files.includes('projectMaverick/loader.js'));assert.ok(r.files.includes('projectMaverick/styles/projectMaverick.css'));for(const f of r.files)assert.doesNotMatch(f,/\.(bin|exe|dll)$/i);});
