const test=require('node:test'),assert=require('node:assert/strict');const{createAdapter}=require('../../src/projectMaverick/core/nativeAdapter');test('normalizes arrays without mutable native references',()=>{const native={politicians:[{id:7,firstName:'Ada',lastName:'Jones',party:'Independent',state:'TX',traits:['Practical']}]},rows=createAdapter(native).readPoliticians();assert.equal(rows[0].id,'7');assert.equal(rows[0].name,'Ada Jones');rows[0].traits.push('Injected');assert.deepEqual(native.politicians[0].traits,['Practical']);});test('safe empty values',()=>{const a=createAdapter({politicians:'bad'});assert.deepEqual(a.readPoliticians(),[]);assert.equal(a.readPlayer(),null);assert.deepEqual(a.readIssueCatalog(),[]);});test('stable fallback ids',()=>{const a=createAdapter({politicians:[{firstName:'Sam',lastName:'Lee',state:'CA',office:'Senator'}]});assert.equal(a.readPoliticians()[0].id,'sam-lee|ca|senator');});
