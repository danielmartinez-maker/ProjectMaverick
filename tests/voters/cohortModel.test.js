const test=require('node:test'),assert=require('node:assert/strict');const{inferCohorts}=require('../../src/projectMaverick/voters/cohortModel');const fixtures=[{partyDistribution:{democrat:.7,republican:.2,independent:.1},fiscalScore:-.5,socialScore:-.6,issueSupport:{healthcare:-.7,environment:-.8}},{partyDistribution:{democrat:.2,republican:.7,independent:.1},fiscalScore:.6,socialScore:.6,issueSupport:{guns:.8,immigration:.7}},{partyDistribution:{democrat:.4,republican:.4,independent:.2},fiscalScore:0,socialScore:0,issueSupport:{}},{partyDistribution:{democrat:.15,republican:.3,independent:.55},fiscalScore:.8,socialScore:-.7,issueSupport:{guns:.9}},{partyDistribution:{democrat:.3,republican:.5,independent:.2},fiscalScore:-.6,socialScore:.7,issueSupport:{welfare:-.8,immigration:.8}}];test('cohort shares are normalized, nonnegative and deterministic',()=>{for(const f of fixtures){const a=inferCohorts(f),b=inferCohorts(f),sum=a.cohorts.reduce((s,c)=>s+c.share,0);assert.ok(Math.abs(sum-1)<1e-10);assert.ok(a.cohorts.every(c=>c.share>=0));assert.deepEqual(a,b);}});test('party context materially shifts coalition composition',()=>{const d=inferCohorts(fixtures[0]),r=inferCohorts(fixtures[1]),dem=c=>c.partisanAffinity.democrat>.7,rep=c=>c.partisanAffinity.republican>.7;assert.ok(d.cohorts.filter(dem).reduce((s,c)=>s+c.share,0)>r.cohorts.filter(dem).reduce((s,c)=>s+c.share,0));assert.ok(r.cohorts.filter(rep).reduce((s,c)=>s+c.share,0)>d.cohorts.filter(rep).reduce((s,c)=>s+c.share,0));});test('missing issues reduce confidence instead of inventing values',()=>{const sparse=inferCohorts({partyDistribution:{democrat:.5,republican:.5,independent:0}}),rich=inferCohorts(fixtures[0]);assert.ok(sparse.confidence<rich.confidence);});
