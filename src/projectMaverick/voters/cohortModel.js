const {COHORT_TEMPLATES}=require('./cohortTemplates');const {scorePolitician,distance}=require('../ideology/scoring');function normalizeParty(p={}){const d=Math.max(0,+p.democrat||0),r=Math.max(0,+p.republican||0),i=Math.max(0,+p.independent||0),s=d+r+i;return s?{democrat:d/s,republican:r/s,independent:i/s}:{democrat:1/3,republican:1/3,independent:1/3};}function inferCohorts(district={}){const party=normalizeParty(district.partyDistribution),ideological=scorePolitician({fiscalScore:district.fiscalScore,socialScore:district.socialScore,issues:district.issueSupport||{}}),raw=COHORT_TEMPLATES.map(t=>{const sim=Math.exp(-2.2*distance(ideological.dimensions,t.profile)),prior=.08+party.democrat*t.partisanAffinity.democrat+party.republican*t.partisanAffinity.republican+party.independent*t.partisanAffinity.independent;return{template:t,weight:sim*prior};}),total=raw.reduce((s,x)=>s+x.weight,0)||1;let running=0;const cohorts=raw.map((x,idx)=>{const share=idx===raw.length-1?Math.max(0,1-running):x.weight/total;running+=share;return{id:x.template.id,name:x.template.name,share,profile:x.template.profile,salience:x.template.salienceWeights,partisanAffinity:x.template.partisanAffinity,stylePreferences:x.template.stylePreferences};}),partyEvidence=district.partyDistribution?1:0,issueEvidence=Object.keys(district.issueSupport||{}).length,confidence=Math.min(1,.25+.35*partyEvidence+.25*Math.min(1,issueEvidence/5)+.15*(district.fiscalScore!=null&&district.socialScore!=null?1:0));return{cohorts,confidence,constraints:{partyDistribution:party,ideology:ideological.dimensions}};}module.exports={inferCohorts,normalizeParty};
