const {comparePoliticianRecords}=require('../qol/compare');const {el,labeledValue}=require('./components');function compareViewModel(a,b){return comparePoliticianRecords(a,b);}function renderComparePanel(doc,target,a,b){const vm=compareViewModel(a,b);target.append(el(doc,'h2','pm-section-title',`${a?.name||'Unavailable'} vs ${b?.name||'Unavailable'}`));for(const[k,v]of Object.entries(vm.dimensionDeltas))target.append(labeledValue(doc,k,(v>=0?'+':'')+v.toFixed(2)));return vm;}module.exports={compareViewModel,renderComparePanel};
