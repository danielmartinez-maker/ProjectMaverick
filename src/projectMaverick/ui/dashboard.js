const {el,button,clear}=require('./components');function renderDashboard(doc,target,ctx){const title=el(doc,'h2','pm-section-title','Political Intelligence'),input=el(doc,'input','pm-search');input.id='pm-search';input.type='search';input.placeholder='Search politician, office, state, party, ideology, archetype';const list=el(doc,'div','pm-list');target.append(title,input,list);function draw(){clear(list);const rows=ctx.searchPoliticians(input.value,ctx.records).slice(0,200);for(const r of rows){const card=el(doc,'div','pm-card'),meta=[r.party,r.office,r.state,r.ideology?.primary?.name,r.politicalStyle?.primary?.name].filter(Boolean).join(' · ');card.append(el(doc,'strong','pm-person-name',r.name),el(doc,'div','pm-muted',meta),button(doc,'Open',()=>ctx.openPolitician(r)));list.append(card);}}input.addEventListener('input',draw);draw();return{refresh:draw};}module.exports={renderDashboard};
