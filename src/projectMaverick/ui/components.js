function el(doc,tag,className,text){const node=doc.createElement(tag);if(className)node.className=className;if(text!=null)node.textContent=String(text);return node;}function labeledValue(doc,label,value){const row=el(doc,'div','pm-row');row.append(el(doc,'span','pm-label',label),el(doc,'span','pm-value',value==null||value===''?'Unavailable':value));return row;}function button(doc,label,onClick,className='pm-button'){const b=el(doc,'button',className,label);b.type='button';b.addEventListener('click',onClick);return b;}function clear(node){while(node.firstChild)node.removeChild(node.firstChild);}module.exports={el,labeledValue,button,clear};
