function createWatchlists(storage){return{list:()=>storage.load().watchlists?.politicians||[],toggle(id){id=String(id);return storage.update(s=>{s.watchlists=s.watchlists||{politicians:[]};const list=s.watchlists.politicians||[];s.watchlists.politicians=list.includes(id)?list.filter(x=>x!==id):[...list,id];return s;});},has(id){return this.list().includes(String(id));}};}module.exports={createWatchlists};
