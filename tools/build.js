const fs=require('node:fs');const path=require('node:path');function walk(dir,base=dir,out=[]){for(const n of fs.readdirSync(dir)){const p=path.join(dir,n),s=fs.statSync(p);if(s.isDirectory())walk(p,base,out);else out.push(path.relative(base,p).replaceAll('\\','/'));}return out;}function build(outDir=path.join(process.cwd(),'dist')){fs.rmSync(outDir,{recursive:true,force:true});const dst=path.join(outDir,'projectMaverick');fs.mkdirSync(dst,{recursive:true});fs.cpSync(path.join(process.cwd(),'src/projectMaverick'),dst,{recursive:true});const files=walk(outDir),prohibited=files.filter(f=>/[.](bin|exe|dll)$/i.test(f)||/(^|\/)audio\//i.test(f));if(prohibited.length)throw new Error(`Prohibited build payload: ${prohibited.join(', ')}`);return{files,prohibited};}if(require.main===module){const r=build(process.argv[2]);console.log(`Built ${r.files.length} files`);}module.exports={build};
